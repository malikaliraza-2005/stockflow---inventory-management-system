/**
 * Walking-skeleton root (Phase 0, task 0.3). The router, AppShell, and guard
 * spine arrive in Phase 1 (FEA §5, §13 implementation order); this component
 * exists so the build / typecheck / component gates have a real target and the
 * staging deploy (M0) serves a verifiable page.
 */
export default function App() {
  return (
    <main>
      <h1>StockFlow — Inventory Management System</h1>
      <p>Phase 0 walking skeleton — M0</p>
    </main>
  );
}
