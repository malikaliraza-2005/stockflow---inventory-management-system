/** Store barrel (SMA §11 folder contract). Components import FACADES
 *  (useToast, useConfirm, usePermission) where one exists — raw stores are for
 *  guards, interceptors, and tests. */
export * from './authStore';
export * from './uiStore';
export * from './settingsStore';
